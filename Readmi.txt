Простые типы данных:
-Числа 1,2,3 (2(53 степень макс))
-Строки 'string', 'name'
-Логический тип (boolean)
-true/false
-null
-undefined
-Symbol
-BigInt

Обьекты:
-Спец обьекты:

-Массивы []
-Функции function
-Обьект Даты
-Регулярное выражение
-Ошибки

--Обычные обьекты



git init
git status
git add -a (добавить все)
git commit -a -m "сообщение комита"
git push (выгрузить в гит)

git pull (обновить локальный реп)
ctrl + c ctrl + c ":wq! - выход из терм
git merge 